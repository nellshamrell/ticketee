  <%= form_for [@ticket, @comment] do |f| %>
  <strong>New comment</strong>
  <%= f.error_messages %>
  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>
  
  <% authorized?(:"change states", @project) do %>
    <p>
      <%= f.label :state_id %>
    </p>
  <% end %>
  
  <%= f.submit %>
<% end %>
